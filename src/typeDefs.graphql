type Phone {
    description: String
    number: String!
}

type Contact {
    id: Int!
    name: String!
    phone: [Phone!]!
    surname: String
}

type Query {
    allContacts: [Contact!]!
    totalContacts: Int!
}
