type Phone {
    description: String
    number: String!
}

type Contact {
    id: Int!
    name: String!
    phones: [Phone!]!
    surname: String
}

type Query {
    allContacts: [Contact!]!
    totalContacts: Int!
}
