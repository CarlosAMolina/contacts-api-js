type Phone {
    description: String
    number: String!
}

type Contact {
    id: Int!
    name: String!
    nicknames: [String!]!
    phones: [Phone!]!
    surname: String
}

type Query {
    allContacts: [Contact!]!
    totalContacts: Int!
}
